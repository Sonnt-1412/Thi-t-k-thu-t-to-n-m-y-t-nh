#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 1e5;
int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    int n = inf.readInt(1, 1000000, "n");
    inf.readEoln();  // kết thúc dòng n

    vector<int> a = inf.readInts(n, -1000000, 1000000, "a_i");
    inf.readEoln();  // kết thúc dòng a_i

    inf.readEof();    // kết thúc file, không còn l,r


}
